const wrapper = {
  '.MuiDialog-paper': {
    borderRadius: 2,
    bgcolor: 'background.paper',
    backgroundImage: 'none',
    width: '100%',
  },
}

const menuStyles = {
  wrapper,
}

export default menuStyles
